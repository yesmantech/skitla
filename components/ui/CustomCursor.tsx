/**
 * @file CustomCursor.tsx — Custom cursor effect stub.
 *
 * @todo Implement a premium custom cursor that:
 *   - Follows the mouse position with smooth interpolation (lerp)
 *   - Shows a circle/dot that reacts to hover states
 *   - Grows/changes on interactive elements (buttons, links)
 *   - Hidden on mobile via CSS (`lg:cursor-none` in layout.tsx)
 *   - Uses requestAnimationFrame for smooth 60fps tracking
 */

"use client";
export function CustomCursor() { return null; }
