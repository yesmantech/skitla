import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // This helps Next.js understand the project structure in nested workspaces
  }
};

export default nextConfig;
